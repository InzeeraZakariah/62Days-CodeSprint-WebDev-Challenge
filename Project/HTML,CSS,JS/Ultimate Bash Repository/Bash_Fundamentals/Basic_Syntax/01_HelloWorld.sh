#!/bin/bash
# This is the "Hello, World!" of Bash scripting.

# The first line, `#!/bin/bash`, is called a "shebang".
# It tells the system that this file should be executed using the Bash interpreter.

# The `echo` command is used to print text to the terminal.
echo "Hello, World!"

# To run this script:
# 1. Save it as a file (e.g., hello.sh).
# 2. Make it executable: chmod +x hello.sh
# 3. Run it: ./hello.sh